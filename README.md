# slackblocks

This repository contains the Python `slackblocks` package in [`python/`](python/)
and the language-neutral JSON conformance contract in [`spec/`](spec/).

The workspace layout leaves room for additional language implementations while preserving the existing Python package and release history.
