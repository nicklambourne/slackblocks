import LanguageContent, { CSharp, Go, Java, Python, TypeScript } from '@site/src/components/LanguageContent';

# API reference

<LanguageContent>
<Python>

Looking for the exact class or constructor argument to use? The [Python API reference](/reference/python) covers every public class, function, signature, and docstring in the package. Start with the category that matches the payload you are building, then follow the linked types as you go.

</Python>
<TypeScript>

Need the precise setter or returned payload type? The [TypeScript API reference](/reference/typescript) brings together every fluent builder, component, type, and validation error. Use it when you know what you want to build and need the exact TypeScript contract.

</TypeScript>
<Go>

Need the exact constructor, fluent method, JSON type, or validation category? The [Go API reference](/reference/go) documents the full module, including all Block Kit builders, payloads, views, and higher-level components.

</Go>
<Java>

Need the exact builder method, immutable return type, or validation category? The [Java API reference](/reference/java) documents every public concrete builder, Block Kit value, payload, component, and exception, including its parameters and direct Slack Java SDK interoperability.

</Java>
<CSharp>

Need the exact constructor parameter, property type, or validation category? The [C# API reference](/reference/csharp) documents every public Block Kit value, role interface, payload, component, and exception, including each constructor's parameters and the limits they enforce.

</CSharp>
</LanguageContent>
