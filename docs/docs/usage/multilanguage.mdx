import CodeBlock from '@theme/CodeBlock';
import LanguageTabs, { JsonTab, PythonTab, TypeScriptTab } from '@site/src/components/LanguageTabs';
import pyHello from '!!raw-loader!@site/examples/python/section_hello.py';
import tsHello from '!!raw-loader!@site/examples/typescript/section_hello.ts';
import jsonHello from '!!raw-loader!@site/examples/section_hello.json';

# Python and TypeScript

The public APIs are independently idiomatic, while the output and validation categories follow one shared specification.

<LanguageTabs>
<PythonTab>
<CodeBlock language="python">{pyHello}</CodeBlock>
</PythonTab>
<TypeScriptTab>
<CodeBlock language="typescript">{tsHello}</CodeBlock>
</TypeScriptTab>
<JsonTab>
<CodeBlock language="json">{jsonHello}</CodeBlock>
</JsonTab>
</LanguageTabs>

The example files are executed by their native test suites, and both results are compared with the JSON tab.
