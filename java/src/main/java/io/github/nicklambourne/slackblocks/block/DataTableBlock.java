// Generated by java/generator/generate_models.py from spec/model.json.
package io.github.nicklambourne.slackblocks.block;

import com.google.gson.annotations.JsonAdapter;
import io.github.nicklambourne.slackblocks.Buildable;
import io.github.nicklambourne.slackblocks.ValidationException;
import io.github.nicklambourne.slackblocks.internal.BuilderState;
import io.github.nicklambourne.slackblocks.internal.SlackObjectJsonAdapter;
import io.github.nicklambourne.slackblocks.internal.TypedFields;
import io.github.nicklambourne.slackblocks.internal.WireObjects;
import java.util.List;
import java.util.Map;
import java.util.OptionalInt;

/**
 * A sortable, paginated table of raw text, numbers, and rich text.
 *
 * <p>Start with {@link #builder()}, set fields with the named fluent methods, and call {@link
 * Builder#build()} to validate and create an immutable value.
 *
 * <ul>
 *   <li>Required: {@code rows}, {@code caption}.
 *   <li>The first row is the header row and cannot contain rich text.
 *   <li>Every row must have the same number of cells, and all cell text together is limited to
 *       20,000 characters.
 * </ul>
 *
 * @see <a href="https://docs.slack.dev/reference/block-kit/blocks/data-table-block">Slack
 *     reference</a>
 */
@JsonAdapter(SlackObjectJsonAdapter.class)
public final class DataTableBlock implements Block {
  private final Map<String, Object> values;
  private final Map<String, Object> fields;
  private final int hash;

  private DataTableBlock(Map<String, Object> values, Map<String, Object> fields) {
    this.values = values;
    this.fields = fields;
    this.hash = values.hashCode();
  }

  /**
   * Starts a new builder.
   *
   * @return a new builder
   */
  public static Builder builder() {
    return new Builder();
  }

  /**
   * Returns complete table rows in display order. Every row must have the same number of cells.
   *
   * @return the values in order, or an empty list when none were set
   * @throws IllegalStateException if the field was set through a raw wire field to a value this
   *     type cannot represent
   */
  public List<List<DataTableCell>> getRows() {
    return TypedFields.rows(fields, "DataTableBlock", "rows", DataTableCell.class);
  }

  /**
   * Returns the caption that describes the table's contents.
   *
   * @return the value
   * @throws IllegalStateException if the field was set through a raw wire field to a value this
   *     type cannot represent
   */
  public String getCaption() {
    return TypedFields.required(fields, "DataTableBlock", "caption", String.class);
  }

  /**
   * Returns how many rows Slack shows per page.
   *
   * @return the value, or an empty optional when it was not set
   * @throws IllegalStateException if the field was set through a raw wire field to a value this
   *     type cannot represent
   */
  public OptionalInt getPageSize() {
    return TypedFields.optionalInt(fields, "DataTableBlock", "page_size");
  }

  /**
   * Returns the zero-based index of the column whose cells act as row headers.
   *
   * @return the value, or an empty optional when it was not set
   * @throws IllegalStateException if the field was set through a raw wire field to a value this
   *     type cannot represent
   */
  public OptionalInt getRowHeaderColumnIndex() {
    return TypedFields.optionalInt(fields, "DataTableBlock", "row_header_column_index");
  }

  @Override
  public Map<String, Object> toMap() {
    return values;
  }

  @Override
  public boolean equals(Object other) {
    return this == other || (other instanceof DataTableBlock that && values.equals(that.values));
  }

  @Override
  public int hashCode() {
    return hash;
  }

  /**
   * Returns this value's compact Slack JSON.
   *
   * @return compact JSON
   */
  @Override
  public String toString() {
    return toJson();
  }

  /** Fluent builder for {@link DataTableBlock}. Builders are mutable and not thread-safe. */
  public static final class Builder implements Buildable<DataTableBlock> {
    private final BuilderState state = new BuilderState("DataTableBlock", "data_table");

    private Builder() {
      state.set("page_size", 5);
      state.set("row_header_column_index", 0);
    }

    /**
     * Adds complete table rows in display order. Every row must have the same number of cells.
     *
     * <p>Required. Must contain between 2 and 201 items. Each call appends to any rows already
     * added.
     *
     * @param rows rows of {@link DataTableCell} values, in display order
     * @return this builder
     */
    @SafeVarargs
    @SuppressWarnings("varargs") // rows are copied into an Object list, never exposed
    public final Builder rows(List<? extends DataTableCell>... rows) {
      state.append("rows", (Object[]) rows);
      return this;
    }

    /**
     * Sets the caption that describes the table's contents.
     *
     * <p>Required.
     *
     * @param value value for Slack's {@code caption} field
     * @return this builder
     */
    public Builder caption(String value) {
      state.set("caption", value);
      return this;
    }

    /**
     * Sets how many rows Slack shows per page.
     *
     * <p>Must be between 1 and 100.
     *
     * @param value value for Slack's {@code page_size} field
     * @return this builder
     */
    public Builder pageSize(int value) {
      state.set("page_size", value);
      return this;
    }

    /**
     * Sets the zero-based index of the column whose cells act as row headers.
     *
     * <p>Must be at least 0.
     *
     * @param value value for Slack's {@code row_header_column_index} field
     * @return this builder
     */
    public Builder rowHeaderColumnIndex(int value) {
      state.set("row_header_column_index", value);
      return this;
    }

    /**
     * Sets a unique identifier for this block. Slack returns it in interaction payloads, so use a
     * stable value when you need to find the block again.
     *
     * <p>Slack allows at most 255 characters.
     *
     * @param value value for Slack's {@code block_id} field
     * @return this builder
     */
    public Builder blockId(String value) {
      state.set("block_id", value);
      return this;
    }

    /**
     * Sets a Slack field that has no named method yet, such as a field Slack introduced after this
     * release. Prefer the named methods, which are typed and documented.
     *
     * <p>The value is checked at build time like any other field. It must be a string, number,
     * boolean, slackblocks value or builder, or a list or map containing only those.
     *
     * @param field Slack JSON field name
     * @param value JSON-compatible value
     * @return this builder
     * @throws IllegalArgumentException if the field name is empty or the value cannot be written as
     *     Slack JSON
     */
    public Builder wireField(String field, Object value) {
      if (field.isEmpty()) {
        throw new IllegalArgumentException("field must not be empty");
      }
      state.set(field, WireObjects.requireWireValue(field, value));
      return this;
    }

    /**
     * Validates the configured fields and creates an immutable value. The builder can be reused.
     *
     * @return an immutable {@link DataTableBlock}
     * @throws ValidationException if the value breaks a Block Kit rule, such as a missing required
     *     field or an exceeded length limit
     */
    @Override
    public DataTableBlock build() {
      return state.build(DataTableBlock::new);
    }
  }
}
