// Generated by java/generator/generate_models.py from java/generator/model.json.
package io.github.nicklambourne.slackblocks.object;

import com.google.gson.annotations.JsonAdapter;
import io.github.nicklambourne.slackblocks.Buildable;
import io.github.nicklambourne.slackblocks.SlackObject;
import io.github.nicklambourne.slackblocks.ValidationException;
import io.github.nicklambourne.slackblocks.internal.BuilderState;
import io.github.nicklambourne.slackblocks.internal.SlackObjectJsonAdapter;
import java.util.Map;

/**
 * Chooses which interactions with an input send a block_actions payload.
 *
 * <p>Start with {@link #builder()}, set fields with the named fluent methods, and call {@link
 * Builder#build()} to validate and create an immutable value.
 *
 * @see <a
 *     href="https://docs.slack.dev/reference/block-kit/composition-objects/dispatch-action-configuration-object">Slack
 *     reference</a>
 */
@JsonAdapter(SlackObjectJsonAdapter.class)
public final class DispatchActionConfiguration implements SlackObject {
  private final Map<String, Object> values;
  private final int hash;

  private DispatchActionConfiguration(Map<String, Object> values) {
    this.values = values;
    this.hash = values.hashCode();
  }

  /**
   * Starts a new builder.
   *
   * @return a new builder
   */
  public static Builder builder() {
    return new Builder();
  }

  @Override
  public Map<String, Object> toMap() {
    return values;
  }

  @Override
  public boolean equals(Object other) {
    return this == other
        || (other instanceof DispatchActionConfiguration that && values.equals(that.values));
  }

  @Override
  public int hashCode() {
    return hash;
  }

  /**
   * Returns this value's compact Slack JSON.
   *
   * @return compact JSON
   */
  @Override
  public String toString() {
    return toJson();
  }

  /**
   * Fluent builder for {@link DispatchActionConfiguration}. Builders are mutable and not
   * thread-safe.
   */
  public static final class Builder implements Buildable<DispatchActionConfiguration> {
    private final BuilderState state = new BuilderState("DispatchActionConfiguration", "");

    private Builder() {}

    /**
     * Adds the interactions that send a payload: on_enter_pressed or on_character_entered.
     *
     * <p>Each call appends to any values already added.
     *
     * @param values values appended in order
     * @return this builder
     */
    public Builder triggerActionsOn(String... values) {
      state.append("trigger_actions_on", (Object[]) values);
      return this;
    }

    /**
     * Validates the configured fields and creates an immutable value. The builder can be reused.
     *
     * @return an immutable {@link DispatchActionConfiguration}
     * @throws ValidationException if the value breaks a Block Kit rule, such as a missing required
     *     field or an exceeded length limit
     */
    @Override
    public DispatchActionConfiguration build() {
      return state.build(DispatchActionConfiguration::new);
    }
  }
}
