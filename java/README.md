# slackblocks for Java

The Java implementation is under development as part of the coordinated
slackblocks release train. It provides immutable Block Kit values with concrete
fluent builders and integrates directly with Slack's official Java SDK.

The artifact version is kept in lockstep with the Python, TypeScript, and Go
packages. Java is not published independently.
